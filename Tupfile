# Syntax
# Highlight
# Office
# Word
# AKA SHOW

PROGRAM = SHOW.exe
CXX = clang++

CFLAGS = -I headers/
CFLAGS += -Wall -Wextra -Wpedantic
CFLAGS += -std=c++17 --target=x86_64-pc-windows-gnu

!CXX = |> $(CXX) -c -o %o %f $(CFLAGS) |>
!LD = |> $(CXX) -o %o %f $(CFLAGS) |>

: foreach src/*.cpp |> !CXX |> obj/%B.o {OBJS}
: {OBJS} |> !LD |> build/$(PROGRAM)
